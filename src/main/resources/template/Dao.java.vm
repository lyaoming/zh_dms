package ${package}.mapper;

import ${package}.entity.${className}Entity;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Dao extends BaseDao<${className}Entity> {
    void saveBatch(List<${classname}Entity> ${classname}List);
}
